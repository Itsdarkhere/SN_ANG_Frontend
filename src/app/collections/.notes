// LEFTOVER CODE – create-collection.component.ts

// let nftPostHashHex = nftFormControlArray["value"]["PostHashHex"];

// function insertPostIntoArray() {
//   this.selectedNfts().insert($event["i"], this.fb.control($event["post"]));
// }

// let currentFormControlHash = nftFormControlArray[i]["value"]["PostHashHex"];

// if(this.selectedNfts()["controls"].includes(this.selectedNfts[FormControl].value)) {
//   console.log("true");
// } else {
//   this.selectedNfts().insert($event["i"], this.fb.control($event["post"]));
//   console.log("false");
// }

// console.log(this.selectedNfts()["controls"]);

//addNftToFormArray($event: object) {
    // let nftFormControlArray = this.selectedNfts()["controls"];
    // this.selectedNfts().insert($event["i"], this.fb.control($event["post"]));
    // console.log(nftFormControlArray);
//}

// this.selectedNfts = this.createCollectionForm.controls.views["controls"][1].controls.selectedNfts.value;

// selectedNfts(): FormArray {
//   return this.collectionSelections.get("selectedNfts") as FormArray;
// }

// [selectedNfts]="selectedNfts"
// (nftPostData)="mapUserNftsToFormControls($event)"







// LEFTOVER CODE – collection-selections.component.ts

// @Output() selectedNft = new EventEmitter<{i: number, post: PostEntryResponse}>();

// captureIndexValue(i: number) {
//   this.nftIndex = i;
//   this.indexOfNft.emit(i);
//   console.log(this.nftIndex);
// }

//addPost(i: number, post: PostEntryResponse) {
    //this.isNftSelected = !this.isNftSelected;

    //if(this.isNftSelected) {
      //this.nftCounter += 1;
      // console.log(this.nftCounter);
      //increment counter
      //add css classes
      //emit event to parent to add to array
    // } else {
      // this.nftCounter <= 0 ? this.nftCounter === 0 : this.nftCounter -= 1;
      // console.log(this.nftCounter);
      //decrement counter
      //remove css classes
      //emit event to parent to remove from array
    //}
    // console.log(post);
  //}

// <ng-container *ngIf="this.postData?.length && !isLoading"></ng-container>

// @Output() nftPostData = new EventEmitter<PostEntryResponse[]>();
// @Output() deselectedNft = new EventEmitter<object>();